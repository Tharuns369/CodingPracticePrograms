#include <stdio.h>
#include <string.h>
int main()
{
	int T, n, m, q, i, input;
	char ch;
	scanf("%d", &T);
	while(T--){
		scanf("%d%d%d", n, m, q);
		for(i=0; i<q; i++){
//			gets(ch);
			scanf("    %c", ch);
			scanf("%d", input);
		}
	}
	return 0;
}
